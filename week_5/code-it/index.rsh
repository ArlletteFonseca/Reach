//backend
'reach 0.1';

const [true,false] = makeEnum(3);


const winner = (handAlice, handBob) =>


export const main = Reach.App(() => {
  const Alice = Participant('Alice', {

   });
   const Bob = Participant("Bob", {

   });
   init();

   var outcome = 0;
   invariant(balance() = payment(outcome));
   
});
