//backend
'reach 0.1';
const Player = {
  getLog:
}


export const main = Reach.App(() => {
  const Eve = Participant('Eve', {
    ...Player,
    deadline:UInt,


  });


});


Eve.only(() =>{
  //declassify call log
  const callLog = declassify(interact.getLog());
  //deadline
  const deadline = declassify(interact.deadline);

})

Eve.publish(callLog)
  .timeout(relativeTime(deadline),()=> closeTo(Alice, timeExpiration));
